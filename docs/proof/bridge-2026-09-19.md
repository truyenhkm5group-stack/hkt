# Bằng chứng: cầu nối mở PR bằng danh tính agent

*19/09/2026. Sinh bởi `.github/workflows/agent-open-pr.yml` — lần chạy đầu tiên sau khi PR #28 merge.*

PR chứa tệp này do **`erp-agent-vnx[bot]`** mở, **không** phải `truyenhkm5group-stack`.

Khác #24 ở một điểm quyết định: #24 do bộ chứng minh tự sinh nhánh của riêng nó, nên nó chứng minh
"App mở được PR". Tệp này đi qua **cầu nối dùng chung** — đường mà từ nay mọi phiên Claude dùng —
nên nó chứng minh điều khác hẳn: **một nhánh do phiên đẩy lên vẫn ra PR mang danh tính bot.**

Đối chứng nằm trong chính lượt chạy: script đọc lại PR bằng `GITHUB_TOKEN` (chỉ đọc), không bằng
token App, và thoát khác 0 nếu `user.login` ≠ bot hoặc `user.type` ≠ `Bot`.
